/**
 * Versión web: no hace nada. `expo-share-intent` es un módulo nativo y en el
 * preview de Expo web no existe; compartir desde otra app tampoco aplica.
 */
export function ShareIntentHandler() {
  return null;
}
